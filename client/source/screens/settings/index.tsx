import { Loader } from '#/components/loader';

export default function Settings() {
  return (
    <div
      style={{
        height: '100%',
        display: 'grid',
        placeItems: 'center',
        placeContent: 'center',
      }}
    >
      <Loader style={{ width: '50px' }} />
      <br />
      Under Construction.
    </div>
  );
}
